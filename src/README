Dependencies:
  ghc; cmdargs; executable-path; java >= 5

Java is required for validating XML output against XSD schema.

To install GHC refer to:
  http://www.haskell.org/ghc/distribution_packages

Install monad transformer library (Ubuntu):
  suod apt-get install libghc-mtl-dev

To install cmdargs execute:
  cabal install cmdargs
  cabal install executable-path

If you have no cabal then installt it (Ubuntu):
  sudo apt-get install cabal-install
  cabal update

Once the libraries are in place, compile the translator:
  make

The translator can validate output (XML, Alloy, Clafer). Make sure that XsdCheck and alloy binaries are places in Test/tools.