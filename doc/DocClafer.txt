The Language clafer
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of clafer==

===Literals===










PosInteger literals are recognized by the regular expression
`````digit+`````

PosDouble literals are recognized by the regular expression
`````digit+ '.' digit+ 'e' '-'? digit+`````

PosReal literals are recognized by the regular expression
`````digit+ '.' digit+`````

PosString literals are recognized by the regular expression
`````'"' (char - [""\"] | '\' [""\nt"])* '"'`````

PosIdent literals are recognized by the regular expression
`````letter (letter | digit | '_' | ''')*`````

PosLineComment literals are recognized by the regular expression
`````{"//"} (char - '
')*`````

PosBlockComment literals are recognized by the regular expression
`````{"/*"} (char - '*' | '*'+ (char - ["*/"]))* '*'+ '/'`````

PosAlloy literals are recognized by the regular expression
`````{"[alloy|"} (char - '|' | '|'+ (char - ']'))* {"|]"}`````

PosChoco literals are recognized by the regular expression
`````{"[choco|"} (char - '|' | '|'+ (char - ']'))* {"|]"}`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in clafer are the following:
  | ``F`` | ``G`` | ``U`` | ``W``
  | ``X`` | ``abstract`` | ``after`` | ``all``
  | ``always`` | ``and`` | ``assert`` | ``before``
  | ``between`` | ``disj`` | ``else`` | ``enum``
  | ``eventually`` | ``final`` | ``finally`` | ``finalref``
  | ``finaltarget`` | ``follow`` | ``globally`` | ``if``
  | ``in`` | ``initial`` | ``initially`` | ``let``
  | ``lonce`` | ``lone`` | ``max`` | ``maximize``
  | ``min`` | ``minimize`` | ``must`` | ``mux``
  | ``never`` | ``next`` | ``no`` | ``not``
  | ``one`` | ``opt`` | ``or`` | ``precede``
  | ``product`` | ``some`` | ``sometime`` | ``sum``
  | ``then`` | ``until`` | ``weakuntil`` | ``xor``

The symbols used in clafer are the following:
  | = | [ | ] | <<
  | >> | { | } | `
  | : | -> | ->> | :=
  | ? | + | * | ..
  | | | <=> | => | ||
  | && | ! | < | >
  | <= | >= | != | -
  | / | % | # | <:
  | :> | ++ | , | --
  | ** | & | . | -->>
  | -[ | ]->> | --> | ]->
  | ; | \ | ( | )

===Comments===
There are no single-line comments in the grammar.There are no multiple-line comments in the grammar.

==The syntactic structure of clafer==
Non-terminals are enclosed between < and >. 
The symbols -> (production),  **|**  (union) 
and **eps** (empty rule) belong to the BNF notation. 
All other symbols are terminals.

  | //Module// | -> | //[Declaration]// 
  | //Declaration// | -> | ``enum`` //PosIdent// ``=`` //[EnumId]// 
  |  |  **|**  | //Element// 
  | //Clafer// | -> | //Abstract// //[TempModifier]// //GCard// //PosIdent// //Super// //Reference// //Card// //Init// //Transition// //Elements// 
  | //Constraint// | -> | ``[`` //[Exp]// ``]`` 
  | //Assertion// | -> | ``assert`` ``[`` //[Exp]// ``]`` 
  | //Goal// | -> | ``<<`` ``min`` //[Exp]// ``>>`` 
  |  |  **|**  | ``<<`` ``max`` //[Exp]// ``>>`` 
  |  |  **|**  | ``<<`` ``minimize`` //[Exp]// ``>>`` 
  |  |  **|**  | ``<<`` ``maximize`` //[Exp]// ``>>`` 
  | //TempModifier// | -> | ``initial`` 
  |  |  **|**  | ``final`` 
  |  |  **|**  | ``finalref`` 
  |  |  **|**  | ``finaltarget`` 
  | //Transition// | -> | **eps**
  |  |  **|**  | //TransArrow// //Exp// 
  | //Abstract// | -> | **eps**
  |  |  **|**  | ``abstract`` 
  | //Elements// | -> | **eps**
  |  |  **|**  | ``{`` //[Element]// ``}`` 
  | //Element// | -> | //Clafer// 
  |  |  **|**  | ````` //Name// //Card// //Elements// 
  |  |  **|**  | //Constraint// 
  |  |  **|**  | //Goal// 
  |  |  **|**  | //Assertion// 
  | //Super// | -> | **eps**
  |  |  **|**  | ``:`` //Exp26// 
  | //Reference// | -> | **eps**
  |  |  **|**  | ``->`` //Exp23// 
  |  |  **|**  | ``->>`` //Exp23// 
  | //Init// | -> | **eps**
  |  |  **|**  | //InitHow// //Exp// 
  | //InitHow// | -> | ``=`` 
  |  |  **|**  | ``:=`` 
  | //GCard// | -> | **eps**
  |  |  **|**  | ``xor`` 
  |  |  **|**  | ``or`` 
  |  |  **|**  | ``mux`` 
  |  |  **|**  | ``opt`` 
  |  |  **|**  | //NCard// 
  | //Card// | -> | **eps**
  |  |  **|**  | ``?`` 
  |  |  **|**  | ``+`` 
  |  |  **|**  | ``*`` 
  |  |  **|**  | //PosInteger// 
  |  |  **|**  | //NCard// 
  | //NCard// | -> | //PosInteger// ``..`` //ExInteger// 
  | //ExInteger// | -> | ``*`` 
  |  |  **|**  | //PosInteger// 
  | //Name// | -> | //[ModId]// 
  | //Exp// | -> | //Exp1// //TransArrow// //Exp// 
  |  |  **|**  | //Exp1// 
  | //Exp1// | -> | ``all`` ``disj`` //Decl// ``|`` //Exp1// 
  |  |  **|**  | ``all`` //Decl// ``|`` //Exp1// 
  |  |  **|**  | //Quant// ``disj`` //Decl// ``|`` //Exp1// 
  |  |  **|**  | //Quant// //Decl// ``|`` //Exp1// 
  |  |  **|**  | ``if`` //Exp1// ``then`` //Exp1// ``else`` //Exp1// 
  |  |  **|**  | ``let`` //VarBinding// ``in`` //Exp1// 
  |  |  **|**  | //Exp2// 
  | //Exp2// | -> | ``never`` //Exp3// //PatternScope// 
  |  |  **|**  | ``sometime`` //Exp3// //PatternScope// 
  |  |  **|**  | ``lonce`` //Exp3// //PatternScope// 
  |  |  **|**  | ``always`` //Exp3// //PatternScope// 
  |  |  **|**  | //Exp3// ``must`` ``precede`` //Exp3// //PatternScope// 
  |  |  **|**  | //Exp3// ``must`` ``follow`` //Exp3// //PatternScope// 
  |  |  **|**  | ``initially`` //Exp3// 
  |  |  **|**  | ``finally`` //Exp3// 
  |  |  **|**  | //Exp3// 
  | //Exp3// | -> | //Exp3// ``<=>`` //Exp4// 
  |  |  **|**  | //Exp4// 
  | //Exp4// | -> | //Exp4// ``=>`` //Exp5// 
  |  |  **|**  | //Exp5// 
  | //Exp5// | -> | //Exp5// ``||`` //Exp6// 
  |  |  **|**  | //Exp6// 
  | //Exp6// | -> | //Exp6// ``xor`` //Exp7// 
  |  |  **|**  | //Exp7// 
  | //Exp7// | -> | //Exp7// ``&&`` //Exp8// 
  |  |  **|**  | //Exp8// 
  | //Exp8// | -> | //Exp8// ``U`` //Exp9// 
  |  |  **|**  | //Exp8// ``until`` //Exp9// 
  |  |  **|**  | //Exp9// 
  | //Exp9// | -> | //Exp9// ``W`` //Exp10// 
  |  |  **|**  | //Exp9// ``weakuntil`` //Exp10// 
  |  |  **|**  | //Exp10// 
  | //Exp10// | -> | ``F`` //Exp10// 
  |  |  **|**  | ``eventually`` //Exp10// 
  |  |  **|**  | ``G`` //Exp10// 
  |  |  **|**  | ``globally`` //Exp10// 
  |  |  **|**  | ``X`` //Exp10// 
  |  |  **|**  | ``next`` //Exp10// 
  |  |  **|**  | //Exp11// 
  | //Exp11// | -> | ``!`` //Exp11// 
  |  |  **|**  | //Exp12// 
  | //Exp15// | -> | //Exp15// ``<`` //Exp16// 
  |  |  **|**  | //Exp15// ``>`` //Exp16// 
  |  |  **|**  | //Exp15// ``=`` //Exp16// 
  |  |  **|**  | //Exp15// ``<=`` //Exp16// 
  |  |  **|**  | //Exp15// ``>=`` //Exp16// 
  |  |  **|**  | //Exp15// ``!=`` //Exp16// 
  |  |  **|**  | //Exp15// ``in`` //Exp16// 
  |  |  **|**  | //Exp15// ``not`` ``in`` //Exp16// 
  |  |  **|**  | //Exp16// 
  | //Exp16// | -> | //Quant// //Exp20// 
  |  |  **|**  | //Exp17// 
  | //Exp17// | -> | //Exp17// ``+`` //Exp18// 
  |  |  **|**  | //Exp17// ``-`` //Exp18// 
  |  |  **|**  | //Exp18// 
  | //Exp18// | -> | //Exp18// ``*`` //Exp19// 
  |  |  **|**  | //Exp18// ``/`` //Exp19// 
  |  |  **|**  | //Exp18// ``%`` //Exp19// 
  |  |  **|**  | //Exp19// 
  | //Exp19// | -> | ``max`` //Exp20// 
  |  |  **|**  | ``min`` //Exp20// 
  |  |  **|**  | //Exp20// 
  | //Exp20// | -> | ``sum`` //Exp21// 
  |  |  **|**  | ``product`` //Exp21// 
  |  |  **|**  | ``#`` //Exp21// 
  |  |  **|**  | ``-`` //Exp21// 
  |  |  **|**  | //Exp21// 
  | //Exp21// | -> | //Exp21// ``<:`` //Exp22// 
  |  |  **|**  | //Exp22// 
  | //Exp22// | -> | //Exp22// ``:>`` //Exp23// 
  |  |  **|**  | //Exp23// 
  | //Exp23// | -> | //Exp23// ``++`` //Exp24// 
  |  |  **|**  | //Exp23// ``,`` //Exp24// 
  |  |  **|**  | //Exp24// 
  | //Exp24// | -> | //Exp24// ``--`` //Exp25// 
  |  |  **|**  | //Exp25// 
  | //Exp25// | -> | //Exp25// ``**`` //Exp26// 
  |  |  **|**  | //Exp26// 
  | //Exp26// | -> | //Exp26// ``&`` //Exp27// 
  |  |  **|**  | //Exp26// ``.`` //Exp27// 
  |  |  **|**  | //Exp27// 
  | //Exp27// | -> | //Name// 
  |  |  **|**  | //PosInteger// 
  |  |  **|**  | //PosDouble// 
  |  |  **|**  | //PosReal// 
  |  |  **|**  | //PosString// 
  |  |  **|**  | ``(`` //Exp// ``)`` 
  | //TransGuard// | -> | //Exp1// 
  | //TransArrow// | -> | ``-->>`` 
  |  |  **|**  | ``-[`` //TransGuard// ``]->>`` 
  |  |  **|**  | ``-->`` 
  |  |  **|**  | ``-[`` //TransGuard// ``]->`` 
  | //PatternScope// | -> | ``before`` //Exp11// 
  |  |  **|**  | ``after`` //Exp11// 
  |  |  **|**  | ``between`` //Exp11// ``and`` //Exp11// 
  |  |  **|**  | ``after`` //Exp11// ``until`` //Exp11// 
  |  |  **|**  | **eps**
  | //Decl// | -> | //[LocId]// ``:`` //Exp21// 
  | //VarBinding// | -> | //LocId// ``=`` //Name// 
  | //Quant// | -> | ``no`` 
  |  |  **|**  | ``not`` 
  |  |  **|**  | ``lone`` 
  |  |  **|**  | ``one`` 
  |  |  **|**  | ``some`` 
  | //EnumId// | -> | //PosIdent// 
  | //ModId// | -> | //PosIdent// 
  | //LocId// | -> | //PosIdent// 
  | //[Declaration]// | -> | **eps**
  |  |  **|**  | //Declaration// //[Declaration]// 
  | //[EnumId]// | -> | //EnumId// 
  |  |  **|**  | //EnumId// ``|`` //[EnumId]// 
  | //[Element]// | -> | **eps**
  |  |  **|**  | //Element// //[Element]// 
  | //[Exp]// | -> | **eps**
  |  |  **|**  | //Exp// //[Exp]// 
  | //[TempModifier]// | -> | **eps**
  |  |  **|**  | //TempModifier// //[TempModifier]// 
  | //[LocId]// | -> | //LocId// 
  |  |  **|**  | //LocId// ``;`` //[LocId]// 
  | //[ModId]// | -> | //ModId// 
  |  |  **|**  | //ModId// ``\`` //[ModId]// 
  | //Exp12// | -> | //Exp13// 
  | //Exp13// | -> | //Exp14// 
  | //Exp14// | -> | //Exp15// 


