from jsir.IR import *

c0_a = Clafer("c0_a");
c0_b = Clafer("c0_b").withCard(1, 1);
c0_a.refTo("int");
c0_b.refTo("int");
Constraint(implies(some(glob(c0_b)), equal(joinRef(glob(c0_b)), sum(glob(c0_a)))));
Constraint(equal(joinRef(glob(c0_b)), constant(2)));
defaultScope(1);
stringLength(16);
